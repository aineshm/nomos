// Copy this file to `config.js` and paste your Mapbox access token.
// Get a free token at https://account.mapbox.com/access-tokens/
//
// Without a token the viewer still runs — it just drops the satellite/street
// basemap and shows the road overlay on a dark background. With a token you get
// the real San Francisco map under the cars.
//
// You can also pass the token in the URL:  index.html?token=pk.xxxxx
window.MAPBOX_TOKEN = "PASTE_YOUR_TOKEN_HERE";
